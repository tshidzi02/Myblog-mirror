// Adds two numbers (summary kept)
/* STUDY: This explains each symbol. The plus adds. STAR-SLASH is written as *\/ here. */
function add(a, b) { /* STUDY: a and b are numbers */ return a + b; }
export default add;
